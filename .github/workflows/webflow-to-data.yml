# DEAKTIVIERT - ersetzt durch webflow-sync.yml
name: '[DEAKTIVIERT] Webflow to Data'
on:
  workflow_dispatch:
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Deaktiviert. Bitte webflow-sync.yml verwenden.'
