# DEAKTIVIERT - ersetzt durch deploy-astro.yml
# Dieser Workflow wird nicht mehr automatisch getriggert.
name: '[DEAKTIVIERT] Hostinger Deploy'
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Dieser Workflow ist deaktiviert'
        required: false
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Dieser Workflow ist deaktiviert. Bitte deploy-astro.yml verwenden.'
